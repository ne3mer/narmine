# 🚀 راهنمای سریع: راه‌اندازی سیستم اعلان‌رسانی

## ✅ کارهایی که انجام شده:

1. ✅ Email Service با Nodemailer
2. ✅ Telegram Bot Service
3. ✅ Notification Sender Service
4. ✅ Integration با Order Service
5. ✅ Integration با Price Alert Service
6. ✅ Email Templates (تأیید سفارش، تحویل، هشدار قیمت)
7. ✅ Telegram Templates

---

## 📝 کارهایی که باید انجام دهید:

### ۱. نصب Dependencies

```bash
cd backend
npm install
```

### ۲. تنظیم Email (SMTP)

**برای تست سریع با Gmail:**

1. به [Google Account](https://myaccount.google.com/) بروید
2. Security → 2-Step Verification را فعال کنید
3. App Passwords → یک password برای "Mail" بسازید
4. در `.env` اضافه کنید:
   ```env
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_USER=your-email@gmail.com
   SMTP_PASS=your-app-password
   ```

**برای Production (توصیه می‌شود):**
- از SendGrid یا Mailgun استفاده کنید
- جزئیات در `NOTIFICATION_SETUP.md`

### ۳. تنظیم Telegram Bot

1. به [@BotFather](https://t.me/BotFather) در Telegram پیام دهید
2. `/newbot` را ارسال کنید
3. نام و username برای bot انتخاب کنید
4. Token را کپی کنید
5. Chat ID خود را از [@userinfobot](https://t.me/userinfobot) بگیرید
6. در `.env` اضافه کنید:
   ```env
   TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
   TELEGRAM_CHAT_ID=123456789
   ```

### ۴. راه‌اندازی مجدد سرور

```bash
cd backend
npm run dev
```

---

## 🎯 نحوه کار:

### خودکار:
- ✅ وقتی سفارش ایجاد می‌شود → ایمیل/تلگرام تأیید ارسال می‌شود
- ✅ وقتی ادمین اطلاعات اکانت را وارد می‌کند → ایمیل/تلگرام تحویل ارسال می‌شود
- ✅ وقتی قیمت بازی کاهش می‌یابد → هشدار ارسال می‌شود

### دستی:
از طریق API یا در کد می‌توانید اعلان ارسال کنید.

---

## 🔍 تست:

1. یک سفارش تست ایجاد کنید
2. بررسی کنید که ایمیل دریافت شده باشد
3. اگر Telegram تنظیم کرده‌اید، پیام را بررسی کنید

---

## ⚠️ نکات مهم:

1. **SMTP credentials را در `.env` تنظیم کنید**
2. **Telegram Bot Token را در `.env` تنظیم کنید**
3. **CLIENT_URL را برای لینک‌های موجود در ایمیل تنظیم کنید**
4. **برای Production از SMTP Server حرفه‌ای استفاده کنید**

---

## 📚 راهنمای کامل:

برای جزئیات بیشتر، فایل `NOTIFICATION_SETUP.md` را مطالعه کنید.

---

**پس از تنظیم، سیستم به صورت خودکار کار می‌کند! 🎉**

